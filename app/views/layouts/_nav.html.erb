<nav class="navbar navbar-dark navbar-expand-lg bg-dark">
  <div class="container-fluid">
    <%= link_to 'MESSAGE BOARD', root_path, class: "fw-bold navbar-brand" %>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
        <% if user_signed_in? %>
          <%= link_to 'Messages', messages_path, class: 'nav-link active' %>
        <% end %>
        </li>
      </ul>
      <form class="d-flex">
        <%= link_to 'Sign Up', new_user_registration_path, class: 'btn btn-outline-primary ' %>
        <% if user_signed_in? %>
          <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-outline-danger ms-1'%>
        <% else %>
          <%= link_to 'Login', new_user_session_path, class: 'btn btn-outline-success ms-1' %>
        <% end %>
      </form>
    </div>
  </div>
</nav>