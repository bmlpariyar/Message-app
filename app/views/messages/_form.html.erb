<%= form_with(model: message) do |form| %>
    <% if message.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(message.errors.count, "error") %> prohibited this message from being saved:</h2>

        <ul>
          <% message.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div>
      <%= form.label :title , class: "form-label", style: "display: block" %>
      <%= form.text_field :title, class: "form-control" %>
    </div>

    <div>
      <%= form.label :discription, class: "form-label", style: "display: block" %>
      <%= form.text_area :discription, class: "form-control", style: "height: 120px" %>
    </div>

    <div>
      <%= form.button :submit , class: "btn btn-primary my-1" %>
    </div>
<% end %>
