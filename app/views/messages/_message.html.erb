<div id="<%= dom_id message %>">
  <h2><%= message.title %></h2>
  <p>Posted by: <i class="fs-5"><%= @message.user.email%></i> <%= time_ago_in_words(@message.created_at)%> ago</p>
  <p><%= message.discription %></p>
</div>
